///The copyWithin() method copies elements to another position in an array, overwriting the existing values

let cars=["skoda","volvo","VW","BMW","BENZ","TATA"]

const copy=cars.copyWithin(2,0,2);

console.log(copy);