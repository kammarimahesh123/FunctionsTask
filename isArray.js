///// This method will checks if an object is an array or not and returns a boolean value.
const fruits = ["Banana", "Orange", "Apple", "Mango"];

let isArray=Array.isArray(fruits)

console.log(isArray);